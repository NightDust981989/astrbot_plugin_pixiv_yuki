# 更新日志

## [v1.1.0] - 2026-05-15
- 新增 LLM 工具调用（Function Calling）支持，AI 可通过自然语言自动调用 `pixiv_random` 和 `pixiv_illust`
- 新增插件配置项 `show_image_info`，支持在管理面板控制随机图片是否显示作品信息（标题、作者、标签），默认开启
- 新增插件配置项 `default_image_size`，支持设置随机图片默认尺寸（mini/thumb/small/regular/original），默认 original